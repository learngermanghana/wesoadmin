rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Testing mode: any authenticated user can read/write
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
